<div class="animated-gradient min-h-screen">
  <slot />
</div>

<style>
  .animated-gradient {
    position: relative;
    z-index: 0;

    background-image: linear-gradient(45deg, #fbc2eb 0%, #a6c1ee 50%, #ffecd2 100%);
    background-size: 200% 200%;
    animation: gradient-shift 7s ease alternate infinite;
    overflow: hidden;
  }

  .animated-gradient::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: -1;

    background-image: linear-gradient(45deg, #000000 0%, #1a1a2e 33%, #16213e 67%, #0f3460 100%);
    background-size: 200% 200%;
    animation: gradient-shift 7s ease alternate infinite;

    opacity: 0;
    transition: opacity 0.5s ease-in-out;
  }

  :global(.dark) .animated-gradient::before {
    opacity: 1;
  }

  @keyframes gradient-shift {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
</style>
